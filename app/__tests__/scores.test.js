import { getPartysScores } from "../src/utils/score";

// describe("test scroe calculation", () => {
//   test("test normal", () => {
//     const inputData = {
//       dflgkndfsglkmn: "sdfkjongdsf",
//     };

//     const result = [];

//     expect(getPartysScores(inputData)).toBe(result);
//   });
// });

describe("test bidon", () => {
  test("normal", () => {
    expect(1).toBe(1);
  });
  test("not normal", () => {
    expect(2).toBe(1);
  });
});
